# Example multi-market Sanity Studio with single dataset

### Demonstrates:

- simple `company` and `offer` schema types
- configuring workspaces for markets
- setting initial value templates
- filtering structure
- filtering references
